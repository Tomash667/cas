// call with runner -e f nest_a_string.txt
void f(string c)
{
	string o;
	o += c[0];
	for(int i=1; i<c.length(); ++i)
	{
		o += '(';
		o += c[i];
	}
	for(int i=1; i<c.length(); ++i)
		o += ')';
	println(o);
}

f("test");
pause();


ctor string
set_global 0
pop
push_local 0
push_arg 0
str_get_char 0
str_add_char
pop

init:
push_int 1
set_local 1
pop

if:
push_local 1
push_arg 0
callu string.length
jnl end

// kod


// inc
push_local 1
inc
set_local 1
pop
jmp if

end:
