no_refcount - brak liczenia referencji
no_instance - nie mo¿na stworzyæ w script


register:
+ reference type
	+ ref counting
	+ no ref count
	+ no instance
+ value type


struct VEC3 {};
value type
	ctor not required
	
	VEC3 f();
	const VEC3 f(); - works same as "VEC3 f();"
	VEC3& f();
	const VEC3& f();
	VEC3* f();
	const VEC3* f();

class Unit - ref type, no instance (need use Create or else)
	ctor not required, addref release required

class RoomInfo - ref type
	ctor addref release required

class Engine - no ref count


no_ref = no_instance
