struct A
{
	int index;
	
	A(int _index)
	{
		index = _index;
		println("A,"+index);
	}
	
	void operator = (A a)
	{
		index = a.index + 5;
		println("A =, "+a.index+"->"+index);
	}
}

void deref(A& a)
{
	A c = a;
	c = A(30);
}
A a = A(40);
deref(a);

pause();
